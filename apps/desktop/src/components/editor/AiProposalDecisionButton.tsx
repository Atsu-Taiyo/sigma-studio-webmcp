"use client";

/**
 * 既存のeditor配下の参照を保ちながら、共通AIデザインシステムの判断ボタンを公開する。
 * 新しい表示面では `components/ui/ai` から直接利用できる。
 */
export { AiProposalDecisionButton, type AiProposalDecision } from "@/components/ui/ai/AiProposalDecisionButton";
