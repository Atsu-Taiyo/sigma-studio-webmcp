// Must match the `.ai-apply-removing`/`.ai-apply-removing-shape` and
// `.ai-apply-added`/`.ai-apply-added-shape` animation durations in globals.css.
export const AI_APPLY_REMOVE_ANIMATION_MS = 280;
export const AI_APPLY_ADD_FLASH_MS = 1200;

export const AI_SIDEBAR_WIDTH = 380;
